# フェーズ 9: TUI・最近使ったファイル

## 前提

- 依存フェーズ: 5（ゲームが動くこと）。8 と並行可能（recent の永続化は T8-1 の config 機構を使うので T8-1 完了後が楽）。
- BluePrint: 「Claude Code などに見られる TUI も提供する」「--recent : 最近使ったファイルを表示して選択できる。ROM ファイルの指定がある場合は、そちらを優先」

## ゴール

`bbl` を引数なしで起動すると TUI が立ち上がり、ROM 選択 → ゲーム起動 → 終了までを完結できる。
`--recent` で履歴からの選択ができる。

## フェーズ完了の検証コマンド

```sh
./bbl              # TUI 起動 → ROM 選択 → プレイ → 戻る → q（一巡、目視）
./bbl --recent     # 履歴から選択できる
./bbl --recent <rom>   # ROM 指定が優先される
odin test tests -collection:bbl=src   # recent 履歴の単体テスト PASS
```

## TUI の設計方針（実装前に読むこと）

- 依存ライブラリなし。ANSI エスケープシーケンス + ターミナル raw mode を自前で扱う（`src/app/tui.odin` に閉じる）
- 画面構成（Claude Code 風 = 枠付きパネル + ステータスライン + キーヒント常時表示）:
  ```
  ┌─ BubiBoyLite v0.1.0 ──────────────────────┐
  │  ROM を選択してください                     │
  │  ▸ ~/roms/game1.gbc          (MBC5, CGB)  │
  │    ~/roms/game2.gb           (MBC1)       │
  │    [ディレクトリを開く…]                    │
  └───────────────────────────────────────────┘
   ↑↓ 選択  Enter 起動  q 終了
  ```
- ゲーム実行中は SDL ウィンドウが主役。TUI は起動元ターミナルにステータス行（FPS / 音量 / スロット）を出し続ける
- 端末が TTY でない（パイプ等）場合は TUI を起動せずエラーメッセージ + exit 1

---

### T9-1: ターミナル制御基盤

- [x] 完了

**目的**: raw mode・ANSI 描画・リサイズ対応の TUI 基盤を作る。
**作るもの**: `src/app/tui.odin`:
- raw mode: POSIX は termios（`ECHO|ICANON` off、`VMIN=0/VTIME=1`）、Windows は `SetConsoleMode`（`ENABLE_VIRTUAL_TERMINAL_PROCESSING` 有効化含む）
- 代替スクリーンバッファ (`\x1b[?1049h/l`)、カーソル非表示、終了時の確実な復元（defer + シグナル/パニック時も）
- 描画: 全画面を文字列バッファに構築して一括 write（ちらつき防止）
- キー入力: 矢印（CSI シーケンス）、Enter、Esc、q の読み取り
- 端末サイズ取得とリサイズ追従
**参照**: TUI 設計方針（上記）。Odin の `core:sys/posix`（termios）/ `core:sys/windows`
**完了条件 (DoD)**: デモ画面（枠 + カーソル移動）が表示され、q で元のターミナル状態に完全復元される。`bbl --headless` 等の非 TUI 経路に影響なし。
**検証方法**:
```sh
./bbl               # TUI デモが出て q で復元
./bbl -v            # TUI を経由せず従来どおり
echo | ./bbl        # 非 TTY: エラーメッセージ + exit 1
```
**落とし穴**: 復元漏れするとユーザーのターミナルが壊れる。異常終了パスでも必ず復元する。
**【実装時の訂正】** 当初の記述「Odin の defer はパニックでも走る」は誤り(実装中に検証: panic()/assert() は
アンワインドせず即 trap するため defer は一切実行されない)。正しくは `context.assertion_failure_proc` を
上書きして panic 直前にフックする方式を使う(tui.odin 冒頭コメント参照)。また index out-of-range 等の
境界チェック違反は assertion_failure_proc すら経由せず `runtime.trap()`(実測 SIGTRAP)へ直行するため、
SIGINT/TERM/HUP に加えて SIGILL/SIGTRAP/SIGSEGV/SIGBUS/SIGABRT/SIGFPE のシグナルハンドラも必須
(これらもセーフティネットとして実装・検証済み)。os.exit は defer を飛ばすので TUI 起動中の exit は
tui_exit_process() 経由にする。Windows 10 の古いコンソールでは VT 処理の有効化が必須(Windows 実機未検証、検証ログ参照)。
**依存**: なし

---

### T9-2: ROM 選択画面

- [x] 完了

**目的**: TUI のメイン画面。ディレクトリの ROM 一覧から選択して起動する。
**作るもの**: tui.odin:
- 起動ディレクトリ（設定 `rom_dir` があればそこ）の `.gb/.gbc` を列挙、ヘッダを読んで MBC 種別/CGB フラグを右側に表示（T4-1 の cartridge.odin を再利用）
- ↑↓ 選択、Enter で起動、ディレクトリ移動（`..` 含む）、q 終了
- 選択して Enter → TUI を**一時停止**（画面復元）→ SDL でゲーム実行 → ゲーム終了後 TUI に戻る
**参照**: BluePrint.md「特徴」、`src/core/cartridge.odin`（T4-1）
**完了条件 (DoD)**: `bbl` 引数なしで TUI → ROM 選択 → プレイ → ウィンドウクローズ → TUI に戻る → q 終了、が一巡する。
**検証方法**: 上記フローを目視で一巡。
**落とし穴**: ROM 一覧のヘッダ読みはファイル先頭 0x150 バイトだけ読む（巨大ディレクトリでの全読み込み禁止）。
**依存**: T9-1

---

### T9-3: 最近使ったファイル履歴と --recent

- [x] 完了

**目的**: BluePrint の `--recent` を実装する。
**作るもの**: `src/app/recent.odin`:
- 履歴ファイル: 設定ファイルと同じ場所に `bbl_recent.txt`（1 行 1 パス、最新が先頭、最大 20 件、重複は先頭へ移動）
- ROM 起動成功のたびに更新。存在しなくなったパスは表示時にスキップ
- `--recent`: TUI の ROM 選択画面を履歴リストで開く。**ROM 引数があれば --recent は無視してそちらを起動**（BluePrint 仕様）
- TUI 通常起動時も「最近使ったファイル」セクションを一覧の先頭に表示
**参照**: BluePrint.md「--recent」
**完了条件 (DoD)**: 単体テスト: 履歴の追加/重複/上限。結合: `./bbl --recent` で履歴選択、`./bbl --recent rom.gb` は rom.gb を直接起動。
**検証方法**:
```sh
odin test tests -collection:bbl=src
./bbl --recent
./bbl --recent <rom>   # ROM 優先
```
**落とし穴**: 履歴のパスは絶対パスで保存。**実ユーザー名を含むパスをログや画面に出すのは可（ユーザー自身の端末）だが、リポジトリのテストフィクスチャに残さない**。
**依存**: T9-1（一覧 UI）, T8-1（設定場所の解決を共有）

---

### T9-4: 実行中ステータス表示

- [x] 完了

**目的**: ゲーム実行中、起動元ターミナルに有用な情報を出す（BluePrint「TUI を活かすアイデア」）。
**作るもの**: tui.odin / main.odin:
- 1 行ステータス（1 秒毎更新、`\r` 上書き）: `▶ game.gbc | 59.7 fps | vol 80% | slot 1 | MBC5+RAM | 双速`
- fps は直近 1 秒の実フレーム数。オーディオアンダーラン発生時は警告色で表示
- TUI 経由でない起動（`bbl rom.gb`）でも TTY なら表示（非 TTY なら出さない）
**参照**: BluePrint.md「TUIを活かすアイデアがあると助かります」
**完了条件 (DoD)**: 実行中に fps・音量・スロットが実際の状態と連動して表示される（目視）。
**検証方法**: `./bbl <ゲーム>` でステータス行を確認、`./bbl <ゲーム> > /dev/null` で出ないこと。
**落とし穴**: stdout はシリアル出力等のログと衝突しうるので、ステータス行は stderr に出す。
**依存**: T9-1

---

### T9-5: TUI からのステート・音量操作

- [x] 完了

**目的**: 実行中のターミナル側でもエミュ操作できるようにする。
**作るもの**: main.odin のイベントループにターミナル入力（非ブロッキング read）を統合:
- `+`/`-` 音量、`1-4` スロット選択、`s` 保存、`l` 復元、`p` 一時停止/再開
- 操作結果は T9-4 のステータス行に反映
- SDL ウィンドウ側のショートカット（F5 等）と同じ内部 API を呼ぶ（二重実装しない）
**参照**: T7-4（ショートカットの内部 API）
**完了条件 (DoD)**: ターミナルから音量・ステート操作ができ、SDL 側ショートカットと同じ結果になる（目視）。
**検証方法**: 実行中にターミナルで各キーを押して確認。
**落とし穴**: ターミナル read はメインループを止めない（VMIN=0/VTIME=0 の poll）。
**依存**: T9-1, T9-4, （ステート操作は T7-4）

---

### T9-6: TUI 統合検証

- [ ] 完了

**目的**: フェーズ 9 のマイルストーン。
**作るもの**: デバッグと修正のみ。チェックリスト:
- [x] `bbl` 引数なし → TUI → 選択 → プレイ → 戻る → q（1 サイクル完走）
      **確認済み(2026-07-17、ユーザー実機検証、2段階)**: まず T9-6着手中に発見した
      クラッシュバグ(temp_allocator関連)修正後、TUI起動→ROM一覧表示→ディレクトリ移動→
      ROM選択→起動を実機確認。続けて「ウィンドウを閉じてTUIへ戻る」で別のバグ(SDL
      ウィンドウの幽霊化、下記参照)を発見・修正し、再度実機で「直りました」の確認を得た。
      これで1サイクル完走を実機確認済み。
- [x] `bbl --recent` が履歴表示、`bbl --recent rom.gb` は ROM 優先
      (T9-3検証ログ参照。pty経由で履歴表示・空履歴メッセージ・ROM優先の3パターン確認済み)
- [x] Ctrl+C・SDL ウィンドウ強制クローズ・パニックのいずれでもターミナル状態が復元される
      **部分確認**: Ctrl+C(SIGINT)とpanic(明示的panic()呼び出し・境界チェック違反による
      SIGTRAPの両方)はpty経由でtermios完全一致を確認済み(T9-1/T9-2検証ログ)。
      **追加確認(2026-07-12)**: ステータス行/ホットキー(T9-4/T9-5)側の統合動作をptyで
      直接検証した。TUI経由でROMを選択しEnterで起動、1秒毎にステータス行
      (`▶ zzz_acid2.gb | NN.N fps | vol 100% | slot 1 | ROM ONLY`)がstderr上で
      `\r`+`\x1b[K`により実際に上書き更新されることを確認。続けて`+`キー送出後、次の
      ステータス行に`| Volume 100%`が反映されること、`p`キー送出後は先頭アイコンが
      `▶`→`⏸`に変わり`| Paused`が付与されFPS計測が0付近まで落ちる(一時停止でフレーム
      進行が止まる設計どおり)ことを確認した。
      **完全確認(2026-07-17、ユーザー実機検証)**: 「SDLウィンドウの強制クローズ」
      (ウィンドウのXボタン)を実機でテストしたところ、ウィンドウが閉じずマウスカーソルが
      スピナー表示のまま固まるバグを発見(下記の検証ログ参照)。原因はアプリ側の
      イベントループ停止ではなく、`video_destroy`が`SDL_DestroyWindow`+`SDL_Quit`を
      呼んでもCocoa側にウィンドウの消去処理が1周も回らないまま終了するため、画面上に
      ウィンドウの見た目だけが残ってしまう`sdl2-compat`(HomebrewのSDL2の実体、SDL3上の
      互換シム)固有の問題と特定(`sample`コマンドでの実機スタックトレース採取、
      Terminal側のTUIが実際には正常に応答していることの確認を経て切り分けた)。
      `video_destroy`に`SDL_HideWindow`+`SDL_PumpEvents`を追加して修正し、実機で
      「直りました」の確認を得た。これによりウィンドウ強制クローズでもターミナル
      (TUI)側の状態が正しく保たれ操作を継続できることを実機確認済み。
- [x] 非 TTY（`echo | bbl`, リダイレクト）で TUI が起動せず適切なメッセージ
      (`echo | ./bbl` で「TUI を起動できません: 標準入出力が端末(TTY)に接続されていません」+
      exit 1 を繰り返し確認済み)
- [ ] macOS Terminal / Linux (xterm 系) で表示崩れなし（Windows Terminal はフェーズ 10 の CI 後に確認）
      **macOS Terminalは実質確認済み(2026-07-17)**: ユーザーがmacOS Terminal上で
      複数回にわたりTUI(ROM一覧・ディレクトリ移動・ゲーム起動・終了)を実際に操作し、
      表示崩れの報告は一切無かった。**Linux(xterm系)は未確認のまま残る**
      (この開発環境・ユーザー環境ともmacOSのみで、Linux実機での目視確認手段が無い)。
**参照**: BluePrint.md
**完了条件 (DoD)**: チェックリスト全項目 + 検証ログに記録。
**検証方法**: 各項目を手動実行。
**現状(2026-07-17更新)**: 5項目中4項目が実機確認済み(うち2件はユーザーの実機テストで
致命的バグ2件を発見・修正した上での確認)。残り1項目(Linux xterm系での表示崩れ確認)のみ
Linux実機での目視確認手段が無く未確認のまま。
**落とし穴**: 「戻る」時の SDL 完全終了（SDL_Quit）と再初期化の繰り返しでリークしないこと（2 周目以降の起動も確認）。
**依存**: T9-2, T9-3, T9-5

---

## 検証ログ

2026-07-17 T9-6着手中に致命的バグを発見・修正: ユーザーに`./bbl`の実機検証を依頼したところ
「1は何も表示されません」と報告(T11-5準備中、フェーズ0/2/9の残タスク着手の一環)。
pty経由で自ら再現したところ、`tui.odin`のROM一覧描画(`tui_run_rom_browser`)が起動直後に
`Index 0 is out of range 0..<0`でクラッシュすることを確認(シグナルハンドラにより端末は正常復元
されるが、画面には何も表示されないまま終了する。ユーザー報告の「何も表示されない」と一致)。

原因調査(pty上でのバイナリ再現テストを多数回実施、advisor相談を経てASan診断を試みたが
Odin dev-2026-07ツールチェインのcompiler-rtとシステムclangのバージョン不一致でASanリンクが
失敗したため断念、代わりに手動bisectionで切り分け): `-o:speed`(最適化あり)ビルドでのみ再現し
`-debug`ビルドでは再現しない。最小化の結果、「`build_recent_entries`内で`recent_load`
(`os.read_entire_file`を呼ぶ)等のファイルI/Oを経由した後、`tui_run_rom_browser`のレンダー
ループが`make([]List_Item, len(entries), context.temp_allocator)`で`context.temp_allocator`
を使ってヒープ確保する」という組み合わせで、この`make`が実際には`len(entries)`件確保した
はずが長さ0のスライスを返す(内部的にtemp_allocatorの状態が壊れている)ことを突き止めた。
スタンドアロンの最小再現コード(`make([]T, N, context.temp_allocator)`単体)では再現せず、
`entries`自体の内容は常に正しい値を保っていたため、実際のヒープ破壊(OOBライトによる
アロケータメタデータ破壊)ではなく、`-o:speed`固有のcontext.temp_allocator関連の
コンパイラ/ランタイムレベルの不具合と判断した(dev-2026-07という開発版ツールチェイン
であることを踏まえ、これ以上の原因追及はOdinコンパイラ自体のデバッグが必要な規模と判断し
深追いを打ち切った)。

**修正**: `tui_run_rom_browser`・`tui_run_recent_browser`(2箇所)の`items`確保を
`context.temp_allocator`から`context.allocator`(ヒープ)+`defer delete(items)`に変更
(`items`は`if dirty`ブロック内でのみ使われ、ブロックを跨いで生存する必要が無いため
安全に heap alloc/free できる)。修正後、pty経由で`./bbl`(引数無し)・`./bbl --recent`
とも `-o:speed`ビルド(デフォルト)・`--release`ビルドの両方で10回以上連続実行し
クラッシュが再現しないことを確認。`odin test tests -collection:bbl=src`: 415 tests
全PASS(回帰なし)。

**注記**: この修正はT9-6の残りチェックリスト項目(実端末での目視確認)を代替するものではない。
むしろ「実端末で最初に試した結果クラッシュを検出した」ことがT9-6着手の直接のきっかけであり、
実機検証の価値を裏付ける結果になった。ユーザーには再度、修正後のバイナリでの実機検証を依頼する。

（タスク完了ごとに 1 行追記）

2026-07-12 T9-1 完了: `odin build src/app -collection:bbl=src -out:bbl -debug` 成功、
`odin test tests -collection:bbl=src` 395件全パス(純粋関数のtui単体テスト13件追加)。
DoDコマンド3点実行: `./bbl -v`(TUI経由せず従来通り表示)、`echo | ./bbl`(非TTYでエラー+exit 1)、
`./bbl --headless`(TUI経路に影響なし)。
目視できないデモ画面(枠+カーソル移動+q復元)は、Python(`pty.openpty`)で疑似端末を確保し
子プロセスとしてbblを起動、矢印キー(down,down,up)とqのバイト列を送り込んで検証した:
(1) 描画されたANSIバッファを文字列として解析し、カーソルマーカー(▸)が期待した行
(down,down,up後は項目2)に来ていることを確認、(2) `termios.tcgetattr`でraw mode化前後の
端末属性が起動前後で完全一致(ECHO/ICANON含む全フィールド)することを確認(stty -gの
プログラム的等価チェック)。Ctrl+C(SIGINT)・意図的なpanic()・意図的なindex out-of-range
(境界チェック違反、assertion_failure_procを経由しないruntime.trap経路)の3パターンでも
同様にpty経由で強制終了させ、いずれも端末状態(termios全フィールド)が起動前と一致することを
確認した(検証用の一時的なテストキーはコミット前に削除済み)。
**実装中に発見した2件のバグと修正**:
(1) `tui_plat_disable_raw`で`tcsetattr(..., .TCSAFLUSH, ...)`を使うと「未転送出力の
drainを待つ」ため、端末側の読み出しが止まっている瞬間に呼ぶと無期限にブロックする
(pty経由の自動テストで再現)。復元時はdrain不要なので`.TCSANOW`に変更して解消。
(2) デモループが毎回(アイドル中も)全画面writeしていたため高頻度出力になり、上記(1)と
組み合わさると出力バッファが溢れて固まりやすかった。「状態が変化した時だけ描画」する
dirtyフラグ方式に変更(ちらつき防止の本来の意図にも合致)。
また `context.assertion_failure_proc` の代入を`tui_enter()`内で行っていたところ、
Odinの暗黙contextは「代入したprocの残り実行+呼び出し先」にしか伝播せず、呼び出し元
(run_tui)がtui_enter()から戻った後には効かないというOdinの仕様上のバグを実装中に発見・修正
(呼び出し元run_tui自身で代入するよう変更)。上記の訂正内容は本ファイルのT9-1「落とし穴」欄にも反映した。
**未確認**: Windows(SetConsoleMode/ConsoleCtrlHandler経路)は開発機にWindows環境が無いため
実機ビルド・実行未確認(tui_windows.odinのコメントに明記)。実際のターミナルウィンドウでの
目視上の表示崩れ有無(全角文字の折り返し、実端末フォントでの罫線文字の見え方等)も未確認
(pty経由の自動検証はバイト列の構造的検証に留まる)。

2026-07-12 T9-2 完了: `odin build src/app -collection:bbl=src -out:bbl -debug` 成功、
`odin test tests -collection:bbl=src` 401件全パス(cartridge_parse_header_lite 2件、
ROMブラウザ純粋関数・scan_rom_directory 4件を追加)。
`core/cartridge.odin` に `cartridge_parse_header_lite` を追加(既存 `cartridge_parse_header` を
「lite解析 + ROM全体サイズ検証」の2段に分割するリファクタ。既存の
`test_cartridge_rom_smaller_than_declared_header_size` はcgb_flag/titleが未設定であることを
アサートしていないことを事前に確認したうえで実施)。`Config` に `rom_dir` フィールド追加
(bbl.ini生成/パース両対応)。
pty(`pty.openpty`)で合成ROM(有効ヘッダのみ、32KB)3本+サブディレクトリを用意し検証:
(1) 一覧表示: ".."→ディレクトリ(名前順)→ROM(名前順)の順で表示され、MBC種別/CGBフラグの
ラベル("(MBC5, CGB)"「(MBC1)」等)が右寄せで正しく出ることを、送出したANSIバッファの
文字列解析で確認。(2) ↓キーでのカーソル移動、Enterでのサブディレクトリ降下・".."での復帰が
一覧内容込みで正しく反映されることを確認。(3) q終了時のターミナル状態復元(termios全フィールド
一致)を確認。
**実装中に発見したバグ2件と修正**(いずれもmacOSの `malloc: *** error for object ...: pointer
being freed was not allocated` で顕在化、pty経由の自動テストで検出):
(1) ".."エントリの `name` に文字列リテラルをそのまま入れていたため、一覧を解放する際に
ヒープ確保でない静的データを free しようとしてクラッシュしていた → `strings.clone("..")`
に修正。
(2) `defer browser_entries_delete(entries)` / `defer delete(cwd)` は単一のcall文なので
引数(スライスヘッダ/文字列)がdefer文の実行時点で即時評価される(Goのdeferと同じ挙動)。
`entries`/`cwd` はディレクトリ移動時に再代入されるため、単純なdeferだと「最初の値」を
関数末尾でもう一度解放してしまい、既に(移動時のインライン解放で)解放済みの内容を
二重解放していた → `defer { ... }` のブロック形式に変更し、ブロック内の変数参照を
実行時(関数末尾)の通常の変数読み取りにすることで解消。
`src/app/tui.odin` の関連コメントに両方の落とし穴を記録済み。
実際のSDLゲーム起動(Enter→ゲーム実行→ウィンドウを閉じてTUIに戻る)は、この開発環境に
対話的なGUIセッションが無く(`screencapture` が真っ黒な画像しか返さない)、SDLウィンドウへの
実際のキーボードフォーカス/クローズイベントを自動生成する手段が無いため、**目視確認できて
いない**。ROM選択→Enter押下でSDL初期化ログ(`video: 表示倍率 = 4 (出力サイズ 640x576)`)が
出力されクラッシュせずゲームループへ入ることは確認したが、そこから先(実際の描画・
ウィンドウクローズ後にTUIへ戻る一連の流れ)は未確認のまま。この確認はT9-6のマイルストーン
検証時に改めて試みる。

2026-07-12 T9-3 完了: `odin build src/app -collection:bbl=src -out:bbl -debug` 成功、
`odin test tests -collection:bbl=src` 410件全パス(`recent.odin` の単体テスト9件を追加:
parse/render往復、重複移動、上限20件での追い出し、存在しないパスのフィルタ、
load/save往復、record_launch結合)。単体テストのフィクスチャは全て `os.temp_dir()` 配下の
合成パス/実ファイルのみを使用し、実ユーザー名を含む固定パスはコミットしていない。
`src/app/recent.odin` を新規作成(履歴ファイル `bbl_recent.txt`、設定ファイルと同じ場所、
最大20件、重複は先頭へ移動、絶対パスで保存)。`.gitignore` に追加。
TUI統合: 通常起動時はTUI起動ディレクトリ(cfg.rom_dirが空ならカレントディレクトリ)を
「ホーム」とし、そこを表示している間だけ一覧の先頭に「★」接頭辞付きで最近使ったファイル
セクションを表示する(サブディレクトリへ移動すると隠れ、".."で戻ると再表示される)。
`--recent`(ROM引数無し)は`tui_run_recent_browser`でディレクトリ移動を持たないフラットな
履歴一覧を開く。`--recent <rom>`はmain.odinで`opts.rom_path != ""`を先に判定しているため
自動的にROM優先になる(履歴が空のとき「最近使ったファイルの履歴がありません」を表示する
ことも確認)。「ROM起動成功のたびに更新」は、直接起動(`bbl rom.gb`)・TUI経由の両方で
run_rom_window が(os.exit(1)せずに)戻ってきた時点で成功とみなして記録する設計とした。
pty(`pty.openpty`)で検証: (1) `./bbl --recent`が履歴無し状態で適切なメッセージを表示、
(2) `bbl_recent.txt`を手動で用意した状態(recent_record_launchの単体テストで動作自体は
検証済みのため、記録トリガーの手動シミュレーションとして)で通常TUIの初期一覧先頭に
★付きで表示されること、`--recent`単独ではその2件だけのフラット一覧になること、
(3) `./bbl --recent /nonexistent/rom.gbc`はTUIを経由せずROM読み込みエラーで即終了する
(ROM優先)ことをそれぞれ確認。SIGINT・非TTY・`-v`の既存経路への影響が無いことも再確認した。
**未確認**: 実際のROM起動成功(SDLゲームプレイ→ウィンドウクローズ)を経由した
recent_record_launch の呼び出しそのものは、T9-2と同じ理由(この開発環境に対話的GUI
セッションが無くSDLウィンドウを自動操作できない)で目視確認できていない
(recent_record_launch 単体の動作はユニットテストで検証済み、呼び出し箇所も
コードレビューで確認済み)。

2026-07-12 T9-4/T9-5 完了: `odin build src/app -collection:bbl=src -out:bbl -debug` 成功、
`odin test tests -collection:bbl=src` 415件全パス(status_cart_label 1件、
game_key_to_action 4件を追加)。
**進捗更新ルールからの意図的な逸脱について明記**: T9-4(ステータス行)と T9-5(ターミナル
ホットキー・音量操作)は本来別コミットにすべきところ、ステータス行が T9-5 の操作結果
(音量・スロット・一時停止)をその場で表示する設計上、実装が密結合しており(例:
Status_Line構造体のvol/slot/pausedフィールドはT9-5側の状態がないと意味を持たない)、
無理に分割すると一方が未完成な中間状態でビルドが通らなくなる。そのため今回に限り
1コミットにまとめた(コミットメッセージにその旨明記)。
**実装内容**: `src/app/audio.odin` に `volume` フィールド追加(T8-1で追加されていた
bbl.iniのvolumeキーがこれまでどこにも適用されていなかった、という既存のギャップを
ここで初めて解消)。`audio_callback` で音量ゲイン(0-100)を適用、`audio_set_volume`/
`audio_adjust_volume` を SDL_Lock/UnlockAudioDevice で保護しながら実装。
`tui.odin` に `Status_Line`(1秒毎に`\r`+`\x1b[K`で1行上書き、フレームカウントは
実際に描画したフレームのみ加算)、`game_key_to_action`(+/-音量、1-4スロット、s保存、
l復元、p一時停止を解釈する純粋関数)、`tui_game_terminal_begin`(直接起動時に
raw modeを自前で有効化する経路)を追加。`main.odin` の `handle_shortcut_action` は
戻り値に実行結果メッセージを追加(SDLショートカット経路は従来通りshow_status、
ターミナルホットキー経路はその戻り値をstatus_line_set_messageへ渡すことで
「同じ内部APIを呼ぶ、二重実装しない」を満たす。音量調整はSDL側に元々ショートカットが
存在しないため、ここが唯一の実装)。一時停止(p)もSDL側に既存の仕組みが無いため新規実装。
5秒毎の定期診断ログ(T5-6)はステータス行が有効な時は重複するため抑制するようにした。
pty(`pty.openpty`)で確認: TUI経由のROM選択→q終了、SIGINT復元、非TTY判定など既存フローに
regressionが無いことを再確認。
**未確認**(いずれもこの開発環境にSDLウィンドウを操作できる対話的GUIセッションが無いため、
T9-2/T9-3から継続する既知の制約): 「実際のSDL描画ウィンドウの目視」自体
(ピクセル描画やウィンドウの見た目)は確認できていない。

**2026-07-12 追加検証で確定した項目**（当初「未確認」としていたが、pty経由で
stdin/stderrのみを使う経路として直接検証できたため確定させた）:
(1) 実際のゲーム実行中にステータス行が1秒毎に更新されること: TUI経由でROM選択→Enter
起動後、stderr出力を3秒間キャプチャし`\r`+`\x1b[K`で複数回上書きされる
`▶ zzz_acid2.gb | NN.N fps | vol 100% | slot 1 | ROM ONLY`行を確認した(fps値は
無音声デバイス環境でのオーディオ駆動ペーシングの影響で実機と異なる可能性があるが、
行の構造・更新自体は実測)。
(2) `+`(音量)/`p`(一時停止)ホットキーが実際にゲーム実行中に機能すること: `+`送出後の
次のステータス行に`| Volume 100%`メッセージが反映され、`p`送出後は先頭アイコンが
`▶`→`⏸`に変わり`| Paused`が付与、FPS計測が0付近まで落ちる(一時停止でフレーム進行が
止まる設計どおり)ことを確認した。これは`run_rom_window`内の`key_reader_poll`→
`game_key_to_action`→`handle_shortcut_action`→`status_line_set_message`という
統合経路全体が実際に配線されていることの証拠であり、単体テストでは検証できない
「配線ミス」のリスクを潰せた。`-`/`1-4`/`s`/`l`は個別には未実射だが、同一の
`game_key_to_action`経由・同一の`handle_shortcut_action`呼び出し経路であり、
`+`/`p`で経路自体の動作は確認済み。
(3) `./bbl <ゲーム> 2>/dev/null` でステータス行が出ないこと、`./bbl <ゲーム> >/dev/null`
(stdoutのみリダイレクト)ではステータス行が出続けること: 直接ROM起動(`bbl zzz_acid2.gb`)を
pty経由で2パターン実行し確認。stdout→pty/stderr→/dev/null では出力0バイト
(video起動ログ含め一切出ない。video起動ログもeprintfln = stderr出力のため)。
stdout→/dev/null/stderr→pty ではvideo起動ログ・ステータス行とも通常どおり出力される
ことを確認し、実装がstdoutではなくstderrのTTY判定を基準にしている設計どおりに
動作することを実地確認した。

2026-07-12 T9-6 途中経過(未完了): T9-1〜T9-5(タスク5/6)は全て検証コマンド実行済みで
完了。T9-6のチェックリスト5項目のうち、pty経由で自動検証できる2項目
(`--recent`の仕様どおりの動作、非TTY時のエラー+exit 1)は確認しチェック済み。
残り3項目(SDLゲームプレイ→ウィンドウを閉じてTUIへ戻る一巡、SDLウィンドウの実際の
強制クローズでの復元、実端末での目視上の表示崩れ有無)は、この開発環境に対話的GUI
セッションが無く(screencaptureが常に真っ黒な画像を返す、osascriptでのウィンドウ一覧
取得も期待通り機能しない)SDLウィンドウを目視・操作する手段が無いため、実施できなかった。
これはコードの不備ではなく開発環境の制約であり、フェーズファイルT9-6の該当項目に
「未完了(環境制約)」として具体的な状況と次回実施すべき内容を明記した。
PLAN.mdはこのため 🟡(進行中)のまま、タスク数を5/6として据え置く。次回、実際の
ターミナル操作が可能なセッションで残り3項目を実行し、問題なければT9-6をチェックして
フェーズを🟢に更新すること。

2026-07-12 T9-6 追加検証(引き続き未完了): 「SDLウィンドウの目視」が必要な項目以外は
極力自動検証で潰しておく方針で、T9-4/T9-5(ステータス行・ターミナルホットキー)がSDL
ウィンドウの目視無しに検証可能なこと(stderr/stdin経由でありSDLウィンドウの可視性とは
無関係)に気づき、pty経由で追加検証した。TUI経由でROM選択→Enter起動後、stderrを
数秒キャプチャしステータス行(`▶ zzz_acid2.gb | NN.N fps | vol 100% | slot 1 | ROM ONLY`)
が`\r`+`\x1b[K`で複数回上書き更新されることを確認、続けて`+`キー送出で`| Volume 100%`が
反映されること、`p`キー送出で`▶`→`⏸`+`| Paused`+FPS急減(一時停止)を確認した。これにより
`run_rom_window`内の`key_reader_poll`→`game_key_to_action`→`handle_shortcut_action`→
`status_line_set_message`という統合経路の配線自体が実際に機能していることを実証できた
(単体テストは純粋関数のみを見ており、この配線ミスは検出できない種類のバグだった)。
また直接ROM起動(`bbl <rom>`)を`stdout→pty/stderr→/dev/null`と`stdout→/dev/null/stderr→pty`
の2パターンでpty経由実行し、前者は出力0バイト(video起動ログ含め皆無)、後者はvideo起動
ログ・ステータス行とも通常どおり出ることを確認し、T9-4「落とし穴」で意図したとおり
stdoutではなくstderrのTTY判定を基準にしている設計が実地でも機能することを確認した。
この結果を受けてphase-09-tui.mdのT9-6チェックリスト2番目(強制クローズ)とT9-4/T9-5ログの
該当箇所を「部分確認」「確定」に更新した。
**依然として未確認のまま残る項目**(SDLウィンドウそのものへの操作を要するため、対話的
GUIセッションが無いこの環境では実施不能): (1) `bbl`引数なし起動→TUI→ROM選択→実際の
SDL描画・プレイ→ウィンドウを閉じる操作→TUIへ戻る、の一巡を目視で確認すること。
(2) SDLウィンドウのXボタン等での強制クローズによる終了(コードパスはEscキー終了と
同一と設計上確認済みだが、実際にその操作自体を再現する手段が無い)。(3) 実端末
(macOS Terminal/xterm系)での目視上の表示崩れ無し。これら3項目はGUI操作可能な
セッションでの再確認が必要であり、T9-6は引き続き🟡・未チェックのまま据え置く。
PLAN.mdのタスク数(5/6)・状態(🟡)も変更しない。

2026-07-17 T9-6実機検証第2ラウンド、致命的バグ2件を発見・修正(いずれもユーザーの実機
テストがきっかけ。コード上の実装のみでは検出不可能だった):

**バグ1(TUIクラッシュ)**: `./bbl`実行時に何も表示されずすぐ終了する不具合をユーザーが
報告。`context.temp_allocator`関連の問題と特定し修正(詳細は本ファイル前半の該当ログ、
コミット参照)。修正後、TUI起動→ROM一覧表示→ディレクトリ移動→ROM選択→起動までを
実機確認。

**バグ2(SDLウィンドウの幽霊化)**: バグ1修正後、続けてSDLウィンドウの閉じるボタン(×)を
押しても閉じずマウスカーソルがスピナー表示のまま固まる不具合をユーザーが報告。
macOS標準の`sample bbl 3`でメインスレッドのスタックトレースを採取したところ
`key_reader_poll`内の`read()`で止まっていることが判明したが、これ自体は
TUIのROM一覧画面(VTIME=1、意図した100msポーリング)の正常な状態である可能性も
あったため、ユーザーに「ハング中にTerminalへフォーカスを切り替え、TUIが操作できるか」
を確認してもらったところ「TUIに戻り操作できる、qで終了もできる」との回答を得た。
これによりアプリ自体のイベントループは正常に`run_rom_window`から復帰しTUIへ
戻っていることが確定し、問題はアプリのロジックではなく**SDLウィンドウを破棄した後も
macOSの画面上にウィンドウの見た目だけが「幽霊」として残る**ことだと切り分けられた。
ユーザーからの追加情報(「TUIでqを押すとウィンドウも消えて終了できる」)により、
プロセス全体が終了して初めてOSが強制的にウィンドウを回収している状況と判明。
`otool -L`で確認済みのとおりHomebrewの`sdl2`は実体が`sdl2-compat`(SDL2 APIをSDL3上に
実装する互換シム、`libSDL3.0.dylib`も同時にロードされる)であり、この互換層が
`SDL_DestroyWindow`+`SDL_Quit`だけではCocoaにウィンドウ消去処理を1周も回さずに
終了してしまうことが原因と推測した。
修正: `src/app/video.odin`の`video_destroy`に`sdl.HideWindow`+`sdl.PumpEvents`を
DestroyWindow前後に追加し、Cocoaへ消去処理を明示的に1ターン回させるようにした。
`odin build`/`odin test`(415件全パス)で回帰なしを確認後、ユーザーに再度実機テストを
依頼し「直りました」の確認を得た。

これによりT9-6のチェックリスト5項目中4項目(1サイクル完走・強制クローズでの復元・
`--recent`・非TTY)が実機確認済みとなった。残る1項目(Linux xterm系での表示崩れ確認)は
Linux実機での目視確認手段が無いため未確認のまま。T9-6全体としては依然🟡・未完了
(この1項目が残るため)。
